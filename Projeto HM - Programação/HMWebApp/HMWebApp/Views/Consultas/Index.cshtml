@model IEnumerable<HMWebApp.Models.Consultas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.med.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pac.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hr_inicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hr_fim)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.med.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pac.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hr_inicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hr_fim)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConsultasID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConsultasID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConsultasID })
        </td>
    </tr>
}

</table>
